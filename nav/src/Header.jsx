import React from "react";
import ReactDOM from "react-dom";

export function Header() {
  return (
    <div
      style={{
        margin: `12px`,
        fontSize: "xx-large",
        background: `red`,
      }}
    >
      Header
    </div>
  );
}
